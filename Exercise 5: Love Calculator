# 🚨 Don't change the code below 👇
print("Welcome to the Love Calculator!")
name1 = input("What is your name? \n")
name2 = input("What is their name? \n")
# 🚨 Don't change the code above 👆

#Write your code below this line 👇


#turn names into lower case so we can use the .count() operator
lower_name1 = name1.lower()
lower_name2 = name2.lower()

#combine names
sum_names = lower_name1+lower_name2 

#count instances of "TRUE"
t = sum_names.count("t")
r = sum_names.count("r")
u = sum_names.count("u")
e = sum_names.count("e")
#count instances of "LOVE"
l = sum_names.count("l")
o = sum_names.count("o")
v = sum_names.count("v")
e = sum_names.count("e")

#operate on variables by summing them since theyre numbers
first_score = t+r+u+e
second_score = l+o+v+e

#individually turn the first score and second score into strings
love_score = str(first_score) + str(second_score)

#then turn the string back into an integer
total_score = int(love_score)

#use IF statements to add outputs based on total score

if total_score < 10 or total_score > 90:
    print(f"Your score is {total_score}, you go together like coke and mentos.")
elif total_score >= 40 and total_score <= 50:
    print(f"Your score is {total_score}, you are alright together.")    
else:
    print(f"Your score is {total_score}.")
